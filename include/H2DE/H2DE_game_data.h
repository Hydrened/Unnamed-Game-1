#ifndef H2DE_GAME_DATA_H
#define H2DE_GAME_DATA_H

/**
 * Type used to identify the game data
 * \since H2DE-2.0.5
 */
struct H2DE_GameData {
    float gravity = 0.01f;              // Gravity for level objects
    float maxGravity = 0.35f;           // Max gravity for a level object
};

#endif
